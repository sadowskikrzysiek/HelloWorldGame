<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.umg.enjoyhelloworld">

    <application
        android:allowBackup="true"
        android:icon="@drawable/simplelogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/simplelogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnjoyHelloWorld">
        <activity android:name=".DecisionActivity"></activity>
        <activity android:name=".StoryActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>